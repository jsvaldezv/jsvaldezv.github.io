/** @type {import('next').NextConfig} */
const nextConfig = {
    // basePath: "/portfolio",
    output: "export",
    images: {
      loader: "akamai",
      path: "",
    },
    // assetPrefix: "./",
};

export default nextConfig;
