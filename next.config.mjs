/** @type {import('next').NextConfig} */
const nextConfig = {
    basePath: "/jsvaldezv.github.io",
    output: "export",
    images: {
      loader: "akamai",
      path: "",
    },
    // assetPrefix: "./",
};

export default nextConfig;
